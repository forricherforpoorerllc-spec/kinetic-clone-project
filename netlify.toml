[build]
  command   = "bun run build"
  publish   = "dist/client"

# SPA fallback — every request gets the prerendered shell so TanStack Router
# can hydrate and handle routing entirely on the client.
[[redirects]]
  from   = "/*"
  to     = "/_shell.html"
  status = 200
