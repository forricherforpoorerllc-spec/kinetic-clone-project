[build]
  command   = "bun run build"
  publish   = "dist/client"

# TanStack Start SPA mode emits dist/client/_shell.html — every route falls back
# to it so the client router can hydrate and render the requested page.
[[redirects]]
  from   = "/*"
  to     = "/_shell.html"
  status = 200
